---
---

<aside class="callout">
  <slot />
</aside>

<style>
  .callout {
    background: color-mix(in oklch, var(--color-brand) 8%, var(--surface-2));
    border-inline-start: var(--border-size-3) solid var(--color-brand);
    border-radius: 0 var(--radius-2) var(--radius-2) 0;
    padding: var(--size-3) var(--size-4);
  }

  .callout :global(p) {
    margin-block: 0;
  }

  .callout :global(* + *) {
    margin-block-start: 0.5em;
  }
</style>
